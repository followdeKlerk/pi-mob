# @pi-mob/pi-extension

Reserved extension package. The real extension that runs inside
`@earendil-works/pi-coding-agent` is added during the M3/M4 checkpoints once
the durable bridge core and Pi RPC adapter are in place. M1 only pins the
package identity and exposes the manifest helper.
