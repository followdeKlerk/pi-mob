# @pi-mob/protocol-fixtures

Shared generated protocol fixtures used by the TypeScript bridge and Flutter client.

```sh
bun run --cwd packages/protocol-fixtures test
bun run fixtures:check
```

Edit the fixture generator, not files in the generated corpus.
