# @pi-mob/protocol-schema

Canonical TypeBox schemas, TypeScript types, and generated JSON Schema for the pi-mob wire protocol.

```sh
bun run --cwd packages/protocol-schema test
bun run schema:check
```

Edit source definitions rather than generated artifacts.
